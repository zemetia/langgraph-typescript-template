function lyricLlm(): string {
    var preamble: string = "blablabla";
    var llm: ChatCohere = new ChatCohere();
    var promptTemplate: string = "";
    
    let pipe: string = "";
    return pipe; 
}

// di contoller
// pipe = lyricLlm()
// pipe.invoke({})